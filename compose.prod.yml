services:
  web:
    container_name: starter-web
    environment:
      - PORT=4200
    build:
      context: .
      target: production
    restart: unless-stopped
    command: pnpm start:prod --host=0.0.0
    ports:
      - '4200:4200'
