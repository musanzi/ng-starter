services:
  web:
    container_name: starter-web
    build:
      context: .
      target: development
    restart: unless-stopped
    command: pnpm start --host=0.0.0
    ports:
      - '4200:4200'
    volumes:
      - .:/app
      - /app/node_modules
